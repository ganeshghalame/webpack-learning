class Form {
    constructor() {
        // alert('Yay form classes are great no matter what javascript developers say');
        let numbers = [5, 10, 15] .map(number => number*2);
        console.log(numbers);
    }
}

new Form();